#include <iostream>

using namespace std;
int main(int argc, char const *argv[]) {
    // freopen("input.txt", "r", stdin);
    char a;
    a = getchar();
    while (a != -1) {
        putchar(a);
        a = getchar();
    }
    return 0;
}
